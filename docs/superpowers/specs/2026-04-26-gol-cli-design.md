# GOL CLI Tool — Design Spec

**Date:** 2026-04-26
**Status:** Approved
**Location:** `gol-tools/cli/`
**Language:** Go (Cobra framework)

> Current usage note (2026-05-02): AI-facing workflows should use `gol run game`, `gol stop`, `gol reimport`, and `gol debug ...`. Raw `node ai-debug.mjs` commands are internal transport details behind `gol debug`.

## Overview

Unified command-line entry point for all GOL development operations. Replaces the scattered `shortcuts/` scripts, `.zshrc` aliases, and overlapping ai-debug features (boot/teardown/reimport) with a single cross-platform Go binary.

Core commands (run/stop/test/reimport) are native Go with no Node.js dependency. The `debug` command is a passthrough to the existing ai-debug Node.js tool.

## Command Tree

```
gol                                    # alias for `gol run game`
gol run game [--detach] [--windowed] [--force] [-- ...]  # smart boot, headless by default
gol run editor                         # open Godot editor (detached)
gol stop [game|editor]                 # no args = stop all
gol test [unit|integration]            # 2-phase test runner, no args = both
gol reimport [--editor]                # headless reimport + UID ensure
gol debug <args...>                    # passthrough to ai-debug (requires Node.js); use `gol debug help`
gol install                            # build + symlink to PATH
gol version                            # print version, commit hash, build date
gol help                               # auto-generated by Cobra
```

## Path Resolution

### Godot Binary

Resolution order:

1. `GODOT_PATH` env var
2. macOS: `/Applications/Godot.app/Contents/MacOS/Godot`
3. Windows: Steam paths → Scoop shim (`%USERPROFILE%\.local\bin\godot.bat`) → `C:\Program Files\Godot\` → `C:\Program Files (x86)\Godot\`
4. Fallback: `godot` on PATH

If not found: `"Error: Godot not found. Set GODOT_PATH or install Godot."`, exit 1.

### Project Path

Resolution order:

1. `--path` flag (available on all commands)
2. `GOL_PROJECT_PATH` env var
3. Auto-detect: walk up from CWD looking for `gol-project/` sibling or `project.godot` in current dir

## Command Details

### `gol run game [--windowed] [--force]`

Default = **headless**. Headless is faster, uses less resources, and supports `gol debug` commands (screenshot, console, eval) via the IPC bridge.

Smart boot sequence:

1. **Duplicate check** — read PID file (`<project>/.godot/gol-game.pid`), verify process is alive. If running and `--force` not set: print `"Game already running (PID <pid>). Use --force to start another or 'gol stop game' first."`, exit 1. Stale PID file (process dead) is cleaned up automatically.
2. **Ensure import cache** — if `.godot/` directory missing, run `godot --headless --import --path <project>`
3. **Launch** — `godot --headless --path <project>` (or without `--headless` if `--windowed`)
4. **Poll readiness** — watch for ai-debug signal directory (`~/Library/Application Support/Godot/app_userdata/God of Lego/ai_signals/`), poll every 100ms, timeout 30s
5. **Write PID file** — write game PID to `<project>/.godot/gol-game.pid`
6. **Stream output** — pipe stdout/stderr to terminal AND timestamped log at `<project>/logs/game/game-YYYYMMDD-HHMMSS.log`
7. **Print PID** — output `"Game started (PID <pid>)"` once readiness poll succeeds

Log path is relative to the project root (`gol-project/logs/game/`). The `logs/` directory should be in `.gitignore`.

Exit code: game process exit code propagated. PID file is cleaned up on exit.

Flags:
- `--windowed`: launch with GUI window instead of headless
- `--force`: skip duplicate-process check, allow multiple instances

### `gol run editor`

1. Launch `godot --editor --path <project>` as detached process
2. Write PID to `<project>/.godot/gol-editor.pid`
3. No multi-instance restriction
4. No headless option (editor requires a window)
5. Print `"Editor started (PID <pid>)"`, return immediately

### `gol stop [game|editor]`

| Invocation | Behavior |
|---|---|
| `gol stop game` | Kill game process (read PID from `gol-game.pid`) |
| `gol stop editor` | Kill editor process (read PID from `gol-editor.pid`) |
| `gol stop` | Kill all — read both PID files |

Process identification: PID file based. Each `gol run` writes a PID file to `<project>/.godot/`:
- `gol-game.pid` — written by `gol run game`
- `gol-editor.pid` — written by `gol run editor`

On stop: read PID file → verify process is alive → kill → remove PID file. If PID file is missing or stale, fall back to process scan (match by process name + command-line args) as a safety net.

Kill sequence: SIGTERM → wait 5s → SIGKILL. Windows: `taskkill /PID` → `taskkill /F /PID`.

Output: `"Stopped game (PID <pid>)"` / `"Stopped editor (PID <pid>)"` or `"No Godot process found"`.

### `gol test [unit|integration] [flags]`

No args = run both phases sequentially. Exit code: 0 if all pass, 1 if any fail.

**Flags:**
- `--suite <names>` — comma-separated suite names to run (e.g. `pcg,ai,system`). Maps to subdirectories under `tests/unit/` and `tests/integration/`. Empty = all suites.
- `--verbose` / `-v` — detailed output with per-suite table and raw gdunit4 output. Default is simplified: failures + summary only.

**Phase 1 — gdUnit4 (unit):**

1. Clean `reports/` directory
2. Run: `godot --headless -s addons/gdUnit4/bin/GdUnitCmdTool.gd --add res://tests/unit/ --ignoreHeadlessMode --verbose`
3. Parse `reports/results.xml` (JUnit XML) using Go `encoding/xml`
4. Capture exit code

**Phase 2 — SceneConfig (integration):**

1. Discover: walk `tests/integration/`, grep for `extends SceneConfig` in each `.gd` file
2. Run each: `godot --headless --path . res://scenes/tests/test_main.tscn -- --config=res://tests/integration/<file>`
3. Capture exit code per file (0 = pass, non-zero = fail)

**ASCII Report:** Same format as current `run-tests.command`:

```
================================================================
                       GOL TEST REPORT
================================================================

  gdUnit4 (Unit)
  ────────────────────────────
  Suite                                  Tests  Pass  Fail    Time
  ──────────────────────────────────────  ─────  ─────  ─────  ───────
  TestHealthComponent                        4      4      0    0.2s
  TestInventoryComponent                     6      5      1    0.3s  <<

  Subtotal: 10   Passed: 9    Failed: 1    Skipped: 0
  Duration: 0.5s

  Failures:
    x TestInventoryComponent.test_stack_overflow

  SceneConfig Integration
  ────────────────────────────
  [PASS] test_enemy_spawn
  [FAIL] test_crafting_recipe

================================================================
  Total: 12   Passed: 10   Failed: 2   Skipped: 0

  RESULT: SOME TESTS FAILED
================================================================
```

### `gol reimport [--editor]`

**Default (headless):**

1. Kill running Godot (same as `gol stop`)
2. Delete `.godot/imported/` directory
3. Run `godot --headless --import --path <project>`
4. Generate missing `.uid` files (walk project tree, create for `.gd`/`.tscn`/`.tres` without one)

**With `--editor`:**

1. Kill running Godot
2. Delete `.godot/imported/`
3. Launch `godot --editor --path <project>`
4. Wait for editor to finish scanning (poll `.godot/imported/` repopulation, timeout 120s)
5. Kill editor process

### `gol debug <args...>`

1. Check `node` on PATH — if not found: `"Error: Node.js is required for 'gol debug'. Install from https://nodejs.org"`, exit 1
2. Resolve ai-debug path: `<repo-root>/gol-tools/ai-debug/ai-debug.mjs` — if not found: `"Error: ai-debug not found at <path>. Is gol-tools submodule initialized?"`, exit 1
3. `exec node ai-debug.mjs <args...>` — replace process, full passthrough (stdin/stdout/stderr)

Common AI-facing debug commands:

```bash
gol debug help
gol debug help input
gol debug screenshot --delay 1
gol debug script .debug/scripts/check_state.gd
gol debug input actions
gol debug input tap interact
gol debug input hold player_right 0.5
```

### `gol install`

1. `go build -o gol .` in `gol-tools/cli/`
2. Determine install path:
   - Check if `~/.local/bin/` exists and is on PATH → use it (preferred, no sudo)
   - Otherwise check `/usr/local/bin/` → use it (may require sudo)
   - Windows: `%USERPROFILE%\.local\bin\gol.exe`
3. Symlink (macOS/Linux) or copy (Windows) the binary
4. Print success message with the install path
5. If the chosen directory is not on PATH, print a hint to add it

### `gol version`

Print version info compiled into the binary via `-ldflags`:

```
gol v0.1.0 (abc1234) built 2026-04-26
```

Fields: semver, git commit short hash, build date. Set at build time via `go build -ldflags "-X main.version=... -X main.commit=... -X main.date=..."`.

## Project Structure

```
gol-tools/cli/
├── main.go                    # entry point, root command
├── go.mod                     # module: github.com/Dluck-Games/gol-cli
├── go.sum
├── cmd/
│   ├── root.go                # root command, global flags (--path, --verbose)
│   ├── run.go                 # `run` parent + `run game` / `run editor`
│   ├── stop.go                # `stop [game|editor]`
│   ├── test.go                # `test [unit|integration]`
│   ├── reimport.go            # `reimport [--editor]`
│   ├── debug.go               # `debug` passthrough
│   ├── install.go             # `install`
│   └── version.go             # `version`
├── internal/
│   ├── godot/
│   │   ├── binary.go          # Godot binary discovery
│   │   ├── process.go         # launch, kill, poll, PID file management
│   │   └── import.go          # headless import, cache clear, UID ensure
│   ├── project/
│   │   └── resolve.go         # project path resolution
│   ├── testrunner/
│   │   ├── runner.go          # orchestrator: discover → execute → report
│   │   ├── gdunit.go          # gdUnit4: invoke + parse JUnit XML
│   │   ├── sceneconfig.go     # SceneConfig: discover + invoke + parse
│   │   └── report.go          # ASCII report renderer
│   └── debug/
│       └── passthrough.go     # Node.js check + exec ai-debug
├── Makefile                   # build, install, clean
└── .goreleaser.yml            # cross-platform release config
```

### Dependency Boundaries

```
Core commands (run/stop/test/reimport)
  └── Godot binary only — no Node.js required

Debug command
  └── Node.js + gol-tools/ai-debug/ai-debug.mjs
  └── Graceful error if Node.js missing

godot-auto-uid.sh hook (post-migration)
  └── Calls `gol reimport` — no Node.js required
```

## Migration Plan

### Delete

The entire `shortcuts/` directory (9 files):
- `run-game.command`, `run-game.bat`, `run-game.ps1`
- `run-editor.command`, `run-editor.bat`, `run-editor.ps1`
- `run-tests.command`, `run-tests.bat`, `run-tests.ps1`

### Add — `.gitignore` (in gol-project)

Add to `gol-project/.gitignore`:
```
logs/
.godot/gol-game.pid
.godot/gol-editor.pid
```

### Update — `.zshrc`

Remove all four aliases (lines 145-149):
```zsh
# DELETE:
alias gol="~/Documents/GitHub/gol/shortcuts/run-game.command"
alias gol-test="~/Documents/GitHub/gol/shortcuts/run-tests.command"
alias gol-editor="~/Documents/GitHub/gol/shortcuts/run-editor.command"
alias godot="~/Documents/GitHub/gol/shortcuts/run-editor.command"
```

After `gol install`, the binary is on PATH — no aliases needed.

### Update — Hook: `godot-auto-uid.sh`

Replace Node.js call with:
```bash
gol reimport
```

### Update — Skill: `gol-debug` (SKILL.md)

| Before | After |
|---|---|
| `node gol-tools/ai-debug/ai-debug.mjs screenshot` | `gol debug screenshot` |
| `node gol-tools/ai-debug/ai-debug.mjs console <cmd>` | `gol debug console <cmd>` |
| `node gol-tools/ai-debug/ai-debug.mjs eval <expr>` | `gol debug eval <expr>` |
| `node gol-tools/ai-debug/ai-debug.mjs reimport` | `gol reimport` |
| `node gol-tools/ai-debug/ai-debug.mjs boot` | `gol run game` |
| `node gol-tools/ai-debug/ai-debug.mjs teardown` | `gol stop` |

Add guidance: "When debugging, prefer `gol run game` (headless) over `--windowed`. Headless is faster, uses less resources, and allows `gol debug` commands for inspection."

### Update — Skill: `gol-test-runner` (runner-prompt.md)

Replace `$GODOT --headless ...` invocations with `gol test unit` / `gol test integration`.

### Update — CLAUDE.md

Replace `run-tests.command` reference with `gol test`.

### Out of Scope

- `gol-tools/foreman/bin/coder-run-tests.sh` — follow-up task
- `gol-tools/foreman/bin/tester-ai-debug.sh` — follow-up task
- `ai-debug.mjs` itself — stays as-is
- `godot-import.mjs` — stays as-is (ai-debug uses it internally)
